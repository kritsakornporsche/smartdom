# 📈 บันทึกความคืบหน้าระบบ SmartDom (Progress Log for Tomorrow)

**วันที่บันทึก:** 29 สิงหาคม 2026  
**เวอร์ชันปัจจุบัน:** `v2.4.0-b120` (`9fc8ce6`)  
**สถานะเซิร์ฟเวอร์:** 🟢 **Online & Ready** (PM2 Service: `smartdom` บนพอร์ต 3000 -> `http://kritsakorn.thddns.net:5993`)  
**สถานะ Git:** 🟢 **Up to date with `origin/main`**

---

## 🎯 สรุปงานทั้งหมดที่ดำเนินการสำเร็จแล้วในวันนี้ (29 ส.ค. 2569)

### 1. ศูนย์จัดการการจองห้องพักครบวงจรสำหรับเจ้าของหอพัก (`/owner/bookings`)
- **ภาพรวมสถิติ (Metrics Dashboard):** การ์ดแสดงจำนวนรายการรออนุมัติ, ยอดรวมเงินประกัน 1 เดือน, สัญญาที่เริ่มใช้งานแล้ว และสถิติการจองทั้งหมด
- **ฟังก์ชันการจัดการคำขอจอง:**
  - **อนุมัติการจอง (Approve):** เปิดสัญญาเช่าเป็น `Active`, ปรับห้องเป็น `Occupied`, อัปเกรดบทบาทเป็น `tenant` พร้อมตัวเลือก **`[✓] ออกใบแจ้งหนี้ค่าเช่าเดือนแรกทันที`**
  - **ปฏิเสธการจอง (Reject):** ระบุเหตุผลการปฏิเสธ คืนห้องพักกลับเป็น `Available (ว่าง)` ทันที
  - **แก้ไขข้อมูลการจอง (Edit):** แก้ไขชื่อ-เบอร์โทรผู้จอง, วันที่สัญญา, ยอดเงินประกัน, และย้ายห้องพักได้
  - **เพิ่มการจอง Walk-in (Manual Booking):** เพิ่มการจองโดยตรงสำหรับนักศึกษาที่มาติดต่อหน้าร้าน พร้อมตัวเลือกอนุมัติทันที
  - **พิมพ์ใบรับเงินจอง (Print Receipt):** ฟอร์แมตเอกสาร A4 สวยงาม แสดงยอดเงินประกัน 1 เดือน พร้อมปุ่มพิมพ์และบันทึกเป็น PDF
  - **ขยายดูสลิปโอนเงิน (Slip Viewer Modal):** ดูรูปสลิปพร้อมเพย์ขนาดเต็ม
- **รองรับหลายหอพัก (Multi-Dorm Selector):** Dropdown สลับดูรายการจองเฉพาะหอพัก หรือดูทุกหอพักรวมกัน (`All Dorms`)

---

### 2. ปรับแต่งประสบการณ์ใช้งานบน Mobile iOS (Apple HIG Compliant)
- **การเลื่อนหน้าจอที่ลื่นไหล:** เพิ่มคลาส `-webkit-overflow-scrolling: touch` พร้อม Safe area bottom padding (`pb-32`)
- **ป้องกัน Safari Auto-Zoom:** ปรับขนาดฟอนต์ของ Form Controls (`input`, `select`, `textarea`) ให้มีขนาดอย่างน้อย 16px
- **Touch Targets 44px:** ขยายขนาดปุ่มกดทั้งหมดให้แตะง่ายตามมาตรฐาน Apple Human Interface Guidelines
- **ปุ่มโทรออกด่วน:** เพิ่มปุ่ม `📞 โทรออก` ข้างเบอร์โทรผู้จอง สำหรับโทรออกผ่านระบบ iOS ทันที

---

### 3. ระบบจองห้องพัก & นโยบายเงินประกัน 1 เดือน (Guest Booking Flow)
- **นโยบายเงินประกัน 1 เดือน:** ปรับยอดชำระล่วงหน้าเหลือเฉพาะ **เงินประกัน 1 เดือน** (ไม่คิดค่าเช่าเดือนแรกตอนจอง ให้เจ้าของหอคิดตอนเข้าพัก)
- **สร้าง QR Code พร้อมเพย์อัตโนมัติ:** คำนวณยอดเงินประกันและดึงเบอร์ PromptPay ของหอพักมาสร้าง QR Code แบบเรียลไทม์
- **แนบสลิปโอนเงิน:** ระบบอัปโหลดรูปภาพสลิปและแสดงตัวอย่างทันทีก่อนกดยืนยันการจอง
- **หน้าตรวจสอบสถานะของผู้จอง (`/tenant`):** แสดงแบนเนอร์ติดตามสถานะการจอง 3 ขั้นตอน พร้อมปุ่มขอยกเลิกการจอง

---

### 4. แก้ไขระบบสแกน QR Code ชำระบิลของผู้เช่า (`/tenant/billing`)
- **แก้ไขปัญหา `Unauthorized (401)`:** ปรับปรุง API `/api/tenant/billing/qr`, `/payment`, `/list` ให้จับคู่ผู้เช่าด้วย `user_id` และ `email` ข้ามทุกหอพักได้อย่างแม่นยำ
- **แก้ไข Error Database Column:** แก้ไขการดึงข้อมูลเบอร์พร้อมเพย์จาก `dormitory_profile` (`promptpay_number`, `promptpay_name`, `name`) ป้องกัน Error 500
- **ปรับปรุง API บิลฝั่งเจ้าของ:** อัปเดต `/api/owner/billing/[id]` และ `/api/owner/billing/batch` ให้รองรับการอัปเดตสลิปและการสร้างบิลอัตโนมัติทุกห้อง
- **Safe Guarded Chunk Reload Handler:** เพิ่ม Global Error Listener ใน `layout.tsx` จัดการ `ChunkLoadError` เมื่อมีการปล่อยอัปเดตใหม่แบบไร้รอยต่อ

---

### 5. ระบบ Versioning อัตโนมัติ (Auto-Increment Versioning)
- สคริปต์ `scripts/generate-version.js` ทำงานอัตโนมัติทุกครั้งที่รัน `npm run build` / `npm run prebuild`
- เพิ่มเลขบิลด์ `buildNumber + 1` ดึง Git Hash ล่าสุด และอัปเดตวัน-เวลาไทยแบบเรียลไทม์
- ป้าย Version Badge มุมขวาล่างแสดงสถานะ `v2.4.0-b120` และเชื่อมต่อไปยังหน้ารายละเอียด `/updates`

---

## 🔑 สรุปรายชื่อบัญชีทดสอบที่พร้อมใช้งาน (Verified Test Accounts)

| บทบาท (Role) | อีเมล / ชื่อบัญชี (Email) | รหัสผ่าน (Password) | รายละเอียดข้อมูลในระบบ | หน้าแดชบอร์ด |
| :--- | :--- | :--- | :--- | :--- |
| **Tenant #1** | `tenant@gmail.com` | `smartdom` | หอพักเกษตร 2 / ห้อง 101 (มี 2 บิล: Unpaid ฿3,850 + Paid ฿3,720) | `/tenant/billing` |
| **Tenant #2** | `tenant@kaset2.com` | `smartdom` | หอพักเกษตร 2 / ห้อง 101 (มี 6 บิลย้อนหลัง) | `/tenant/billing` |
| **Tenant #3 (Kritsakorn)** | `kritsakorn8011@gmail.com` | `smartdom` | หอพักเกษตร 2 / ห้อง 407 (บิล #784 ฿3,900 รอตรวจสลิป + บิล #783) | `/tenant/billing` |
| **Dorm Owner** | `kritsakorn801@gmail.com` | `smartdom` | เจ้าของหอพักหลัก & เกษตร 2 (ดูการจองและบิลได้ครบทุกห้อง) | `/owner/bookings` |
| **Super Admin** | `admin` | `admin` | ผู้ดูแลระบบสูงสุด แพลตฟอร์ม & หอพักทุกแห่ง | `/platform` |

---

## 📋 แผนงานสำหรับดำเนินการต่อในวันพรุ่งนี้ (Next Steps for Tomorrow)

1. **End-to-End User Verification:**
   - ทดสอบโฟลว์การจองของ Guest -> โอนเงินประกัน 1 เดือน -> เจ้าของหอกดอนุมัติที่ `/owner/bookings` พร้อมเปิดบิลค่าเช่าเดือนแรก -> ผู้เช่าล็อกอินสแกนจ่ายค่าเช่าและแนบสลิปที่ `/tenant/billing` -> เจ้าของหอกดยืนยันชำระเงินที่ `/owner/billing`
2. **การทดสอบประสิทธิภาพ (Performance & Lighthouse):**
   - รันสคริปต์ `npm run report:run` เพื่อเก็บข้อมูลคะแนน Performance, Accessibility, Best Practices, SEO
3. **จัดเตรียมเอกสารสรุปผลงานวิจัย / Thesis Report:**
   - นำข้อมูล Log และสรุปฟีเจอร์ลงในเอกสารรายงานผลการทดสอบระบบ
